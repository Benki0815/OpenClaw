# ============================================================
# OpenClaw – Environment Variables (.env)
# ============================================================
# WARNUNG: Diese Datei NICHT committen! Nur .env.example ist im Repo.
# Kopiere diese Datei nach .env und trage deine echten Werte ein:
#   cp .env.example .env
# ============================================================

# --- Gateway ---
# Ein langer, zufälliger Token für die Gateway-Authentifizierung.
# Generieren z.B. mit: openssl rand -hex 32
OPENCLAW_GATEWAY_TOKEN=REPLACE_WITH_RANDOM_TOKEN

# Gateway-Port (Default: 18789, ist frei auf dem NAS)
OPENCLAW_GATEWAY_PORT=18789

# Gateway-Bind (lan = erreichbar im Container-Netzwerk, notwendig für Docker)
OPENCLAW_GATEWAY_BIND=lan

# --- LLM Provider: OpenRouter ---
# API-Key von https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-REPLACE_WITH_YOUR_KEY

# --- Telegram Bot ---
# Bot-Token von @BotFather
TELEGRAM_BOT_TOKEN=REPLACE_WITH_TELEGRAM_BOT_TOKEN

# --- Home Assistant (spätere Phase, auskommentiert) ---
# Long-Lived Access Token von HA: Profil → Security → Long-Lived Access Tokens
# HOMEASSISTANT_TOKEN=REPLACE_WITH_HA_TOKEN
# HOMEASSISTANT_URL=http://host.docker.internal:8123

# --- Pfade (Synology NAS) ---
OPENCLAW_CONFIG_DIR=/volume1/docker/openclaw/config
OPENCLAW_WORKSPACE_DIR=/volume1/docker/openclaw/workspace
